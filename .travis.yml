language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
env:
    - DJANGO=1.10
    - DJANGO=1.11

install:
    - pip install --upgrade pip
    - pip install -r requirements/test.txt
    - pip install Django==$DJANGO
    - pip install .
script:
  - pytest

sudo: false
