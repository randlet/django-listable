language: python
python:
  - "3.5"
  - "3.6"
env:
    - DJANGO=2.1

install:
    - pip install --upgrade pip
    - pip install -r requirements/test.txt
    - pip install Django==$DJANGO
    - pip install .
script:
  - pytest

sudo: false
